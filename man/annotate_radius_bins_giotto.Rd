% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate_radius_bins_giotto.R
\name{annotate_radius_bins_giotto}
\alias{annotate_radius_bins_giotto}
\title{Annotate Cells with Concentric Circle Radius Bins}
\usage{
annotate_radius_bins_giotto(
  gobject,
  data,
  concentric_circles_sf,
  save_output = TRUE,
  metadata_file = "meta.xenium.merge.rds",
  gobject_file = "GiottofullObject_Xenium_sfConcCircles.rds"
)
}
\arguments{
\item{gobject}{A Giotto object containing cell metadata.}

\item{data}{Data frame containing spatial coordinates and cell metadata.
Must contain columns \code{cell_ID}, \code{x.coord.shift}, and
\code{y.coord}.}

\item{concentric_circles_sf}{An sf object containing concentric circle
geometries generated by \code{generate_concentric_circles()}.}

\item{save_output}{Logical. If TRUE, save the annotated metadata and
updated Giotto object as RDS files.}

\item{metadata_file}{Character string specifying the filename used to save
merged metadata.}

\item{gobject_file}{Character string specifying the filename used to save
the updated Giotto object.}
}
\value{
A list containing:
\describe{
\item{xenium_cells_annotated}{sf object after spatial radius assignment.}
\item{xenium_cells_clean_binned}{Data frame containing one radius assignment per cell after removing duplicate assignments.}
\item{meta.xenium.merge}{Merged metadata containing radius annotations for all cells.}
\item{gobject}{Updated Giotto object with \code{radius.concentric.sf} metadata added.}
}
}
\description{
Assigns each cell to the smallest concentric circle radius that contains
it by performing a spatial join between cell coordinates and concentric
circle geometries. Radius annotations are merged back into the parent
Giotto object and can optionally be saved for downstream analyses.
}
\details{
Cells are converted into sf point geometries using the
\code{x.coord.shift} and \code{y.coord} coordinates. A spatial join is
performed against the concentric circle polygons, and if a cell is
assigned to multiple radii, only the smallest radius is retained. The
resulting radius annotation is stored as
\code{radius.concentric.sf}.
}
